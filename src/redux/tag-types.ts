export enum tagTypes {
    user = "user",
    admin = "admin",
    profile = "profile",
    service = "service",
    blog = "blog"
  }
  
  export const tagTypesList = [
    tagTypes.user,
    tagTypes.admin,
    tagTypes.profile,
    tagTypes.service,
    tagTypes.blog
  ];