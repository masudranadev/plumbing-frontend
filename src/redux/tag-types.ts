export enum tagTypes {
    user = "user",
    admin = "admin",
    profile = "profile",
    service = "service"
  }
  
  export const tagTypesList = [
    tagTypes.user,
    tagTypes.admin,
    tagTypes.profile,
    tagTypes.service
  ];